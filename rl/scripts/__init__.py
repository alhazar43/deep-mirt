"""Standalone OrdRec scripts.

Each script under ``rl/scripts/`` is a CLI entrypoint, not a library
module. The package marker exists so the scripts can be imported in
tests without confusing pytest's collection.
"""
